# MEO SEIHA デモ環境（100%静的SPA）利用・デプロイガイド

このデモ環境は、代理店（販売パートナー）様が導入を検討されている店舗様に対して、MEO SEIHAの全自動投稿（下書き自動作成）やAIクチコミ返信の使いやすさを実演するための**100%フロントエンド完結型のデモシステム**です。

## 特徴
1. **サーバー・データベース不要：** すべてブラウザの `localStorage` 上に保存・読み込みされるため、データベースやサーバーを設定することなく、ローカルのHTMLファイルをブラウザで開くか、静的ホスティングに配置するだけで完動します。
2. **Netlifyに即座にデプロイ可能：** ビルドした `dist` フォルダ、または本リポジトリをそのままNetlifyに連携するだけで、完全無料で公開用のMEO SEIHAデモURLを作成できます。
3. **「美容室・ヘアサロン」テーマ（Avenir Hair）初期プリセット：** カウンセリング技術や髪質改善を強みとする上品なヘアサロンを初期値として設定済みです。実機さながらのクチコミ履歴（4件）や、Google Driveを模した美しいヘアケア系の高解像度ストック写真10枚があらかじめ用意されています。
4. **クラウド経由での安全なGemini AI生成（Netlifyサーバーレスプロキシ）：** 
   - Netlifyの管理画面にGemini APIキーを登録（環境変数: `GEMINI_API_KEY`）するだけで、フロントエンドにキーを一切露出させることなく、営業担当者が端末に関わらず即座に本物のGemini AIを実演起動できます。
   - 万が一、APIキーが未登録または通信オフラインの場合でも、自動的にローカルの「高品質な美容室テーマフォールバック生成」が走るため、プレゼン中にAIエラーで画面が止まるリスクが完全に排除されています。
   - 必要に応じて、設定画面から一時的な任意のAPIキーをブラウザのlocalStorageに保存して実演することも可能です（デプロイ設定が不要なため簡単です）。
5. **1タップでのクイックログイン：** ログイン画面下部に「代理店管理画面 ➔」「店舗管理画面 ➔」のショートカットボタンを設置し、ワンタップで即座に各ダッシュボードに入ることができるため、スムーズな提案活動が可能です。

---

## 🚀 起動・ビルド手順

### 1. 依存関係のインストール
```bash
npm install
```

### 2. ローカルでの開発起動（ホットリロード対応）
```bash
npm run dev
```
起動後、ブラウザで `http://localhost:5173/` にアクセスしてください。

### 3. Netlifyなどの公開用ビルド
```bash
npm run build
```
ビルド完了後、ルート直下に `dist` フォルダが作成されます。このフォルダ内のすべてのファイルをNetlify等に静的ホスティングとして配置してください。

---

## 💡 デモ実演のシナリオ案（店舗オーナーへの見せ方）

1. **ワンクリックで店舗ログイン**
   - ログイン画面下部の「店舗デモ（美容室）」ボタンを押して、店舗オーナー用ダッシュボード（美髪改善サロン Avenir Hair）にアクセスします。
2. **Google Drive擬似同期と投稿モード（パターンC）の実演**
   - 「画像」タブを開き、あらかじめ10枚の美しい美容室ストック写真が読み込まれている様子を店主に見せます。
   - 「画像をアップロード」を押してローカルの写真をストックに追加でき、即座にダッシュボードに反映される（localStorageに保存される）様子を見せます。
3. **キーワードに連動した全自動投稿（下書き作成）の実演（コア機能！）**
   - 「設定」タブを開き、美容室のメインキーワード（例：「栄 美容室」「髪質改善」）やフッター、AIへの個別プロンプト（ウリのアピール）を見せます。
   - 「ダッシュボード」に戻り、「AIで下書きを再生成（または個別に再生成）」を押します。
   - **数秒で、設定に完全に連動した箇条書き＆ハッシュタグ＆フッター contact card付きの洗練されたMEO投稿が生成される**様子を実演します。
   - 「自動投稿テスト」をクリックすると、カレンダーが1日スライドして新しい下書き（Day 2）がスライド生成され、Day 0が「本日投稿済み」カードに変わる実機のローテーションの凄さを実演します。
4. **AIクチコミ返信 ＆ LINEアラートの実演**
   - 「クチコミ」タブを開き、星5の高評価（田中様）には既にお礼文が自動で送信されていることを見せます。
   - 星1・星2の低評価クチコミ（佐藤様や渡辺様）には、自動返信はされずに**AIが謝罪文の下書き（お詫び文）を作成して保留中**になっていることを見せます。
   - 指示フォーム（例：「もっとお礼の言葉を強調して」や「次回使えるトリートメントクーポンの説明を添えて」など）に指示を入力して「AIに書き直してもらう」をクリックし、**瞬時に文面が上品にリライトされる**魔法のような様子を実演します。
   - 「LINEテスト送信」ボタンを押すと、実際のLINE緊急アラートと同じメッセージが即時シミュレートされる様子を見せます。
5. **代理店ダッシュボードの実演（代理店スタッフの教育用）**
   - 一度ログアウトし、「代理店デモ」ボタンからログインします。
   - フランチャイズや複数の管理店舗（Avenir Hair など）の稼働ステータスや自動返信ON/OFF状況を一元管理できる、代理店様向けの強固な管理画面を実演できます。

---

## 🔑 Netlifyでの本物AI（Gemini API）連携設定（セキュリティ対応）

各営業担当者様が客先でAPIキーを入力する手間を省き、かつAPIキーの漏洩を完璧に防ぐため、**Netlifyのサーバーレス中継API（`netlify/functions/gemini.ts`）**を使用しています。

### 🔑 クラウンド環境変数への設定手順：
1. Netlifyの管理画面（クラウド上）にログインします。
2. デプロイした本デモ環境サイトの「Site settings」 ➔ 「Environment variables」を開きます。
3. 「Add a variable」をクリックし、以下の通り環境変数を追加します：
   - **Key:** `GEMINI_API_KEY`
   - **Value:** （取得したGoogle Gemini APIキー）
4. 保存すると、すべての営業端末でGemini APIキーを入力することなく、本物のAIによる文章生成・書き直しデモが完全に安全にスムーズに動くようになります！

※APIキーを未登録の間でも、高品質なオフライン擬似生成（フォールバック）が自動的に身代わりで走るため、エラーでプレゼンが止まることは絶対にありません。
